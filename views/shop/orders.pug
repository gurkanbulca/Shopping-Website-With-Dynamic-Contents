extends ../layouts/main-layout.pug

block content   

    if orders.length > 0 
        each order in orders
            .card.mb-5
                .card-header
                    h3 order id: ##{order.user._id}
                table.table
                    thead
                        tr
                            th Image
                            th Product Name
                            th Quantity
                            th Price
                    tbody
                        each item in order.items
                            tr
                                td
                                    img(src=`/img/${item.imageUrl}` width="80")
                                td #{item.name}
                                td #{item.quantity}
                                td #{item.price*item.quantity}
                    tfoot
                        tr
                            td(colspan="2") #{order.date}
                            td
                            - var total = 0;
                            each item in order.items
                                - total += (item.price * item.quantity)
                            td  #{total} TL

                


    else
        h1 No Orders